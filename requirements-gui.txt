# Зависимости приложения настроек (gui_main.py).
# Вынесены отдельно: службе синхронизации PySide6 не нужен, а весит он много.
#
# PySide6-Essentials вместо PySide6: метапакет PySide6 тянет за собой ещё и
# PySide6-Addons — 161 МБ колеса, около 400 МБ на диске (Charts, Multimedia,
# Qt3D, WebEngine, DataVisualization). Приложение импортирует ровно три модуля:
# QtCore, QtGui и QtWidgets. Чего нет на сборочной машине, то не попадёт
# в дистрибутив ни при каких настройках PyInstaller.

PySide6-Essentials==6.11.1
